# ============================================================================
# Swarm Module
# ============================================================================
# This CMakeLists.txt exists to allow add_subdirectory(Swarm) in src/CMakeLists.txt
# The actual Swarm sources and resources are handled there via target_sources()
# and the Swarm.qrc is registered at runtime by the application.